---
import { Home } from "components/HomePage/Home";
import Layout from "layouts/Layout.astro";
import { getAuthToken } from "utils/getAuthToken";

const token = getAuthToken(Astro.cookies);
---

<Layout title="FeedMe">
  <Home client:only="react" token={token} />
</Layout>
